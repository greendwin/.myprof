# A tiny repo with my profiles for vim, bash, etc.

Prerequisites:

```bash
pipx install stow-python
```

WIP:
* rework all scripts and configs to compatibility with `stow`

Old commands to install with script:

```bash
# install bash
git clone https://github.com/greendwin/.myprof ~/.myprof && ~/.myprof/install.sh

# install zsh
git clone https://github.com/greendwin/.myprof ~/.myprof && ~/.myprof/install-zsh.sh
```
